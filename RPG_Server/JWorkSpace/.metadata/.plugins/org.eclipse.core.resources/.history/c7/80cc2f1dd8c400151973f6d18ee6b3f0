package jg.rpg.dao.db;

import jg.rpg.entity.DBEntityInfo;
import jg.rpg.entity.DBHelper;

class DBHandler {
	
	protected DBHandler(DBEntityInfo info){
		DBHelper.getConnection(info.getDriver(), info.getUrl() , info.getUser() , info.getPwd());
	}
	
}
